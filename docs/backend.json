
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in SocialBoost.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "balance": { "type": "number", "default": 0 },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "email", "createdAt"]
    },
    "FundRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FundRequest",
      "type": "object",
      "description": "A request by a user to add money to their wallet.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "amount": { "type": "number" },
        "utrId": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "Approved", "Rejected"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "amount", "utrId", "status", "createdAt"]
    },
    "SMMService": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SMMService",
      "type": "object",
      "description": "A social media service offered on the platform.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "platform": { "type": "string", "enum": ["instagram"] },
        "pricePer1000": { "type": "number" },
        "minQuantity": { "type": "number" },
        "isActive": { "type": "boolean", "default": true }
      },
      "required": ["id", "name", "platform", "pricePer1000", "minQuantity"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "User profile and balance."
        }
      },
      {
        "path": "/fundRequests/{requestId}",
        "definition": {
          "entityName": "FundRequest",
          "schema": { "$ref": "#/backend/entities/FundRequest" },
          "description": "Global fund addition requests."
        }
      },
      {
        "path": "/services/{serviceId}",
        "definition": {
          "entityName": "SMMService",
          "schema": { "$ref": "#/backend/entities/SMMService" },
          "description": "Managed social media services."
        }
      }
    ]
  }
}
