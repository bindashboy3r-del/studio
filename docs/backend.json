
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in SocialBoost.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string" },
        "displayName": { "type": "string" },
        "balance": { "type": "number", "default": 0 },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "email", "createdAt"]
    },
    "FundRequest": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FundRequest",
      "type": "object",
      "description": "A request by a user to add money to their wallet.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "amount": { "type": "number" },
        "utrId": { "type": "string" },
        "status": { "type": "string", "enum": ["Pending", "Approved", "Rejected"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "amount", "utrId", "status", "createdAt"]
    },
    "RedeemCode": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RedeemCode",
      "type": "object",
      "description": "A code that can be redeemed for wallet balance.",
      "properties": {
        "id": { "type": "string" },
        "code": { "type": "string" },
        "amount": { "type": "number" },
        "limit": { "type": "number" },
        "usedCount": { "type": "number" },
        "usedBy": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["code", "amount", "limit", "usedCount", "usedBy"]
    },
    "SMMService": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SMMService",
      "type": "object",
      "description": "A social media service offered on the platform.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "platform": { "type": "string", "enum": ["instagram"] },
        "pricePer1000": { "type": "number" },
        "minQuantity": { "type": "number" },
        "order": { "type": "number", "description": "The display order of the service." },
        "isActive": { "type": "boolean", "default": true }
      },
      "required": ["id", "name", "platform", "pricePer1000", "minQuantity"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": { "$ref": "#/backend/entities/User" },
          "description": "User profile and balance."
        }
      },
      {
        "path": "/fundRequests/{requestId}",
        "definition": {
          "entityName": "FundRequest",
          "schema": { "$ref": "#/backend/entities/FundRequest" },
          "description": "Global fund addition requests."
        }
      },
      {
        "path": "/redeemCodes/{codeId}",
        "definition": {
          "entityName": "RedeemCode",
          "schema": { "$ref": "#/backend/entities/RedeemCode" },
          "description": "Wallet redeem codes."
        }
      },
      {
        "path": "/services/{serviceId}",
        "definition": {
          "entityName": "SMMService",
          "schema": { "$ref": "#/backend/entities/SMMService" },
          "description": "Managed social media services."
        }
      }
    ]
  }
}
